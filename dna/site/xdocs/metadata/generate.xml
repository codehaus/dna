<?xml version="1.0"?>

<document>
    <properties>
        <title>Generating Descriptors</title>
        <author email="peter at apache.org">Peter Donald</author>
    </properties>
    <body>
        <section name="Generating Descriptors">
            <p>
                DNA uses the MetaClass toolkit to generate metadata
                descriptors. The developer should thus familiarize
                themselves with the MetaClass toolkit. The DNA tools
                extends the base MetaClass toolkit so that specific
                dna tags are recognized, validated, populated with
                default values and types resolved. The following ant
                snippet can be used to generate DNA metadata descriptors.
            </p>
            <source>
&lt;taskdef
    name="generatemeta"
    classname="org.jcontainer.dna.tools.metaclass.GenerateDNADescriptorsTask"&gt;
    &lt;classpath refid="dna-tools.class.path" /&gt;
    &lt;classpath refid="metaclass.class.path" /&gt;
    &lt;classpath refid="qdox.class.path" /&gt;
&lt;/taskdef&gt;

&lt;generatemeta dest="target/classes"&gt;
    &lt;fileset dir="src/java"&gt;
        &lt;include name="**/*.java"/&gt;
    &lt;/fileset&gt;
&lt;/generatemeta&gt;
            </source>
            <p>
                The developer may also choose to use the base MetaClass
                descriptor generation task. If the user does this then must
                either specify the interceptor
                <code>org.jcontainer.dna.tools.metaclass.DNAAttributeInterceptor</code>
                or must annotate their source files with fully specified type
                names and must specify all values including default values. 
            </p>
        </section>
    </body>
</document>
